apiVersion: v1
kind: Service
metadata:
  name: colorbox
  namespace: mdata-toolkit
  labels:
    app: colorbox
spec:
  type: ClusterIP
  selector:
    app: colorbox
  ports:
    - port: 80
      targetPort: 5011
      protocol: TCP
